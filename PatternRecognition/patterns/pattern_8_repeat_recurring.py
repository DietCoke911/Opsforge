class Pattern8_RepeatRecurring:
    def detect(self, text):
        # Placeholder for repeat issues
        if "Still Blocked" in text:
            return {"name": "Repeat Recurring Issue", "score_contribution": 20, "evidence": ["Recurring blocked outcome"]}
        return None
